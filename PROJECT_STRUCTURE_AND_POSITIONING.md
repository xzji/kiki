# KiKi 项目架构层级、差异化与演进方向

## 0. 阅读对象与文档目标

本文面向产品经理，用产品和系统设计视角解释 KiKi 的架构，而不是讲代码实现细节。

读完本文后，应能回答三个问题：

- KiKi 的架构层级应该如何划分，每层承担什么产品责任。
- KiKi 和 OpenClaw 相比，真正差异化在哪里。
- KiKi 后续最值得投入的提升方向是什么。

一句话概括：

> KiKi 是一个围绕长期目标持续推进的多 Runtime Agent 执行平台，不是单纯的聊天助手，也不是简单的 CLI Web UI。

---

## 1. KiKi 的架构应该怎么理解

### 1.1 先看产品本质

KiKi 的核心不是“让模型回答问题”，而是完成一个长期闭环：

```text
理解用户目标
  ↓
沉淀上下文和记忆
  ↓
拆解计划和任务
  ↓
调度本地 Runtime 执行
  ↓
回收过程和产物
  ↓
推动用户继续决策
```

所以 KiKi 的架构不能只按“前端、后端、本地 CLI”来分。那样会漏掉两个最关键的产品层：

- 任务编排层：决定目标如何变成可执行任务。
- 记忆与上下文层：决定系统如何长期理解用户、项目和历史。

### 1.2 推荐的整体分层

更适合 KiKi 的分层应该是：

```text
1. 产品体验层
   ↓
2. 目标与任务编排层
   ↓
3. 上下文与记忆层
   ↓
4. 云端控制与状态层
   ↓
5. 事件协议与过程层
   ↓
6. 远程连接层
   ↓
7. 本地执行层
   ↓
8. Runtime 适配层
   ↓
9. AI Runtime 与模型层
```

同时有三条横切能力贯穿全链路：

```text
A. 权限与安全
B. Artifact 与结果治理
C. 观测、审计与恢复
```

### 1.3 为什么这样分

这个分层不是按技术目录划分，而是按 KiKi 的产品能力链路划分：

- 用户看到的是体验层。
- 用户真正购买的是目标推进能力。
- 目标推进依赖任务编排。
- 任务编排依赖上下文和记忆。
- 长期运行依赖云端状态。
- 跨端和本地执行依赖事件协议与远程连接。
- 真实工作发生在本地 Runtime。
- 智能上限来自底层模型。

---

## 2. 每一层做什么，不做什么

### 2.1 产品体验层

这是用户直接看到和操作的层。

**负责什么**

- 展示会话、目标、任务、收件箱、日程、设置等产品界面。
- 承接用户输入，例如提问、创建目标、确认计划、选择 Runtime、查看产物。
- 展示 Agent 的执行过程，例如思考、工具调用、文件生成、错误和等待用户确认。
- 把复杂执行过程翻译成用户能理解的产品状态。

**不负责什么**

- 不直接执行本地命令。
- 不直接读写用户本地文件。
- 不决定任务真实状态。
- 不保存长期业务事实。

**产品意义**

体验层的目标是降低理解成本。用户不应该面对一堆终端日志，而应该看到：当前目标推进到哪一步、哪个任务正在跑、是否需要我确认、最终产物是什么。

---

### 2.2 目标与任务编排层

这是 KiKi 区别于普通聊天助手的核心层。

**负责什么**

- 将用户的自然语言目标转化为结构化目标。
- 对目标进行澄清、拆解、规划和阶段划分。
- 生成可执行任务，并维护任务之间的依赖关系。
- 判断任务什么时候可以执行、是否被阻塞、是否需要用户确认。
- 将执行结果回流到目标进度里，推动下一步行动。
- 把长期目标和短期任务、日程、收件箱连接起来。

**不负责什么**

- 不直接调用本地 CLI。
- 不直接解析 Claude 或 Pi 的底层协议。
- 不直接管理本地文件系统。
- 不把每次模型输出都当成最终计划，必须经过结构化和治理。

**产品意义**

如果没有这一层，KiKi 只是“会聊天、会执行命令”的工具。有了这一层，KiKi 才能变成“能持续推进目标的 Agent”。

这一层回答的是：

- 用户到底想达成什么？
- 目标应该拆成哪些阶段？
- 当前最应该执行哪个任务？
- 哪些任务需要用户确认？
- 执行结果如何改变目标进度？

---

### 2.3 上下文与记忆层

这是 KiKi 能否长期陪伴用户、持续理解项目的关键层。

**负责什么**

- 保存和整理用户偏好、项目规则、会话摘要、任务经验和长期背景。
- 在合适时机为任务提供上下文，而不是每次都从零开始。
- 对长会话和长期目标做摘要、压缩和提炼。
- 区分短期上下文和长期记忆。
- 支持用户查看、修正、删除重要记忆。
- 避免敏感信息进入长期记忆。

**不负责什么**

- 不替代任务编排层做计划。
- 不直接执行任务。
- 不把所有历史原样塞给模型。
- 不把临时日志、敏感数据、一次性噪音都沉淀为长期记忆。

**产品意义**

没有记忆层，Agent 每次都像第一次认识用户，长期目标无法持续推进。

记忆层解决的是：

- 用户偏好是什么？
- 这个项目有哪些长期约束？
- 上次任务做到了哪里？
- 哪些经验下次应该复用？
- 哪些内容只适合短期使用，不应长期保存？

---

### 2.4 云端控制与状态层

这是 KiKi 的“大脑”和“账本”。

**负责什么**

- 管理用户、会话、目标、任务、消息、运行状态和事件记录。
- 作为长期状态的事实源，保证刷新页面、换设备后状态仍可恢复。
- 决定一次任务应该交给哪台本地机器、哪个 Runtime 执行。
- 维护不同 Runtime 的会话关系，避免 Claude 和 Pi 的 session 混用。
- 接收本地执行结果，并写入统一状态体系。
- 处理本地离线、Runtime 不可用、模型超时、权限不足等错误。

**不负责什么**

- 不直接运行本地 CLI。
- 不直接访问用户本地代码仓库。
- 不相信前端传来的 Runtime session 值。
- 不把具体 Runtime 的协议细节暴露给产品界面。

**产品意义**

这一层保证 KiKi 是一个可持续运行的产品，而不是一个依赖当前网页生命周期的临时工具。

---

### 2.5 事件协议与过程层

这是把底层执行过程变成产品体验的翻译层。

**负责什么**

- 将 Runtime 的输出标准化为统一事件。
- 表达任务过程中的关键状态，例如运行中、思考中、调用工具、生成文件、等待确认、出错、完成。
- 支持前端过程展示和任务追踪。
- 支持断线恢复后的过程补齐。

**不负责什么**

- 不决定任务规划。
- 不管理长期记忆。
- 不理解每个 Runtime 的全部私有细节。
- 不让前端直接依赖 Claude、Pi 等 CLI 的原始输出格式。

**产品意义**

事件协议让不同 Runtime 在产品里呈现为同一种体验。否则 Claude 一套展示、Pi 一套展示，产品会很难扩展。

---

### 2.6 远程连接层

这是云端和用户本地机器之间的连接层。

**负责什么**

- 让本地机器主动向云端领取任务。
- 把本地执行过程和结果回传云端。
- 维护本地机器在线状态。
- 支持流式过程回传。
- 支持网络波动后的恢复和重试。

**不负责什么**

- 不理解用户目标。
- 不判断任务是否应该执行。
- 不解析模型输出含义。
- 不决定产品界面如何展示。

**为什么用 HTTP 长轮询**

KiKi 当前更适合 HTTP 长轮询，而不是 WebSocket：

- 本地机器通常没有公网入口，只能主动连接云端。
- 普通 HTTP 在 Railway、代理、防火墙和企业网络里更稳定。
- 任务执行天然像队列：本地取任务，执行后回传。
- 断线后可以通过任务 ID、事件序号和状态记录恢复。
- 实现和运维复杂度低于长期 WebSocket 连接。

**产品意义**

这一层决定用户的本地机器能否稳定成为 KiKi 的执行资源。

---

### 2.7 本地执行层

这是运行在用户电脑上的执行器。

**负责什么**

- 发现本地安装了哪些 Runtime。
- 检查 Runtime 是否可用、是否登录、工作目录是否存在。
- 在用户本地执行任务。
- 读取本地项目上下文。
- 调用 shell、写文件、生成产物。
- 把执行过程和结果回传云端。

**不负责什么**

- 不保存云端业务状态的最终真相。
- 不决定目标和任务的产品状态流转。
- 不承担用户、目标、任务的长期管理。
- 不直接设计前端展示形态。

**产品意义**

这一层让 KiKi 能利用用户真实本地环境完成真实工作。对于代码任务，只有在真实仓库、真实依赖、真实命令环境中执行，结果才有验证价值。

---

### 2.8 Runtime 适配层

这是不同 CLI 的翻译层。

**负责什么**

- 把 KiKi 的统一任务请求翻译成 Claude、Pi 等 CLI 能理解的指令。
- 把不同 CLI 的输出翻译成 KiKi 统一事件。
- 处理不同 CLI 的 session 恢复方式。
- 识别具体 CLI 的错误、重试、超时、空响应等异常。
- 屏蔽不同 Runtime 的协议差异。

**不负责什么**

- 不决定产品状态。
- 不直接更新前端 UI。
- 不把 Claude/Pi 的私有概念扩散到业务层。
- 不承担长期目标规划和记忆治理。

**产品意义**

这一层让 KiKi 具备多 Runtime 扩展能力。未来接入 Codex CLI、Gemini CLI 或企业内部 CLI 时，应新增适配器，而不是重写整个产品。

---

### 2.9 AI Runtime 与模型层

这是实际提供智能能力的底层。

**负责什么**

- 接收指令。
- 调用模型。
- 理解代码和上下文。
- 执行工具。
- 生成文本、代码或文件。

**不负责什么**

- 不理解 KiKi 的完整产品模型。
- 不管理长期目标。
- 不保证产物自动进入 Web UI。
- 不负责跨 Runtime 的统一体验。
- 不负责用户记忆和任务治理。

**产品意义**

Runtime 和模型决定智能能力上限，但 KiKi 的产品价值来自上层如何组织目标、任务、上下文、权限、产物和恢复机制。

---

## 3. 三条横切能力

### 3.1 权限与安全

KiKi 会接触本地文件、shell 命令、代码仓库和用户长期上下文，因此权限不是附属功能，而是基础能力。

**负责什么**

- 管理文件读取、文件写入、shell、网络访问等能力。
- 说明关闭权限后会影响哪些产品能力。
- 记录高风险行为，支持审计。
- 防止敏感路径、临时数据、密钥、调试日志进入云端或长期记忆。

**产品意义**

权限层决定用户是否信任 KiKi。用户需要知道 Agent 有什么能力、做了什么、哪些数据不会被带走。

---

### 3.2 Artifact 与结果治理

Artifact 是 Agent 生成的文件、报告、表格、页面、代码修改或其他交付物。

**负责什么**

- 识别本地执行产生了哪些结果。
- 将结果回流到会话、任务、目标或收件箱。
- 标注产物来源、生成时间、对应任务和可用状态。
- 支持预览、下载、继续编辑和后续任务接力。
- 过滤敏感文件、大文件、临时文件和非交付物。

**产品意义**

Artifact 层让 KiKi 从“会回答”升级为“能交付”。

---

### 3.3 观测、审计与恢复

长期任务一定会遇到失败、断线、超时和用户中断。

**负责什么**

- 记录任务从创建到完成的完整过程。
- 解释任务卡住的原因。
- 支持任务取消、恢复、重试和回放。
- 区分网络问题、Runtime 问题、模型问题、权限问题和业务问题。
- 为产品和研发提供问题定位依据。

**产品意义**

这一能力决定 KiKi 是否可运营、可排障、可持续迭代。

---

## 4. 一条任务在 KiKi 中如何流转

以“帮我完成一个代码任务”为例，完整链路是：

```text
用户提出目标
  ↓
产品体验层接收输入
  ↓
目标与任务编排层判断目标、拆解任务
  ↓
上下文与记忆层补充用户偏好、项目规则、历史经验
  ↓
云端控制与状态层创建任务记录并选择执行资源
  ↓
事件协议与过程层定义过程回传格式
  ↓
远程连接层把任务下发到本地机器
  ↓
本地执行层在用户电脑上运行
  ↓
Runtime 适配层调用具体 CLI
  ↓
AI Runtime 与模型层执行推理和工具调用
  ↓
Artifact 与结果治理层识别产物
  ↓
云端状态更新，前端展示结果和下一步决策
```

这个链路说明：KiKi 的核心不是某一次模型调用，而是一个可持续、可恢复、可解释的任务推进系统。

---

## 5. KiKi 对比 OpenClaw 的差异化

### 5.1 核心差异

KiKi 与 OpenClaw 的差异不是“谁能调用模型”，而是产品主轴不同。

| 对比维度 | KiKi | OpenClaw |
| --- | --- | --- |
| 产品主轴 | 长期目标推进 | 通用 Agent 执行 |
| 用户心智 | 帮我持续推进目标 | 帮我完成一次任务 |
| 任务编排 | 目标、任务、实例、日程、收件箱联动 | 更偏单次任务或执行链路 |
| 记忆体系 | 面向用户、项目、会话、任务的长期记忆治理 | 更偏上下文压缩和执行记忆 |
| 状态管理 | 云端长期保存，服务端是事实源 | 更偏本地或单次执行上下文 |
| 执行位置 | 云端调度，本地执行 | 更偏本地 Agent loop 或固定执行框架 |
| Runtime 策略 | 多 Runtime 可插拔 | 更偏固定执行栈 |
| 结果形态 | 会话、任务、收件箱、日程、Artifact 联动 | 更偏执行结果和过程输出 |
| 产品容器 | 目标、任务、实例、日程、收件箱、记忆、Artifact | Agent 任务和执行过程 |

### 5.2 KiKi 的差异化价值

**长期目标系统**

KiKi 不只是回答问题，而是围绕用户目标持续推进：澄清目标、拆解任务、调度执行、回流结果、提醒用户决策。

**任务编排能力**

KiKi 有目标、任务、实例、日程和收件箱的完整链路，能够把一个长期目标拆成可执行、可追踪、可恢复的任务系统。

**记忆与上下文治理**

KiKi 不只是保留聊天记录，而是要区分用户偏好、项目规则、任务经验和临时上下文，为长期目标提供持续理解能力。

**云端控制面**

KiKi 把长期状态放在云端，把本地能力作为执行资源。这使它更适合跨设备、后台任务、恢复执行和长期追踪。

**多 Runtime 平台化**

KiKi 不绑定单一 CLI。Claude、Pi、未来其他 Runtime 都可以被接入同一套任务系统。

**产品容器更完整**

KiKi 有会话、目标、任务、收件箱、日程、记忆和 Artifact。这些容器让 Agent 结果不只是日志，而是能被管理、追踪和复用。

**结果回流能力**

KiKi 重视把本地生成的文件带回产品界面，让用户能直接看到和使用交付物。

### 5.3 OpenClaw 值得借鉴的地方

OpenClaw 的一些经验仍值得 KiKi 吸收：

- 更强的执行上下文压缩能力。
- 在任务边界触发记忆整理。
- 在上下文过载前主动整理，而不是失败后补救。
- 更细粒度的工具失败重试。
- 更强的本地执行恢复能力。

这些能力可以增强 KiKi 的执行稳定性，但不改变 KiKi 的核心定位：长期目标执行平台。

---

## 6. 当前项目还可以提升什么

### 6.1 强化任务编排层

**为什么重要**

这是 KiKi 从“Agent 工具”变成“目标执行平台”的关键。

**提升方向**

- 统一目标、任务、实例、日程、收件箱之间的关系。
- 建立标准任务状态机：待执行、执行中、等待用户、阻塞、失败、完成、取消、恢复。
- 为任务增加验收标准和复盘机制。
- 支持跨任务结果汇总。
- 让系统主动发现目标推进中的阻塞点。

### 6.2 强化记忆与上下文层

**为什么重要**

长期目标需要长期理解。没有记忆，Agent 每次都像第一次认识用户。

**提升方向**

- 区分用户偏好、项目规则、会话摘要、任务经验和临时上下文。
- 在任务完成、阶段切换、上下文接近上限时自动整理记忆。
- 使用 patch 式更新，避免整段重写造成虚假差异。
- 让用户能查看、编辑和删除长期记忆。
- 避免敏感信息进入长期记忆。

### 6.3 让架构边界更稳定

**为什么重要**

产品越复杂，越容易出现前端、云端、本地执行器都在修改状态的问题。一旦多处同时写状态，就会出现结果不一致、会话错乱、恢复失败。

**提升方向**

- 明确云端是状态事实源。
- 前端只做展示和交互。
- 本地执行器只做执行和回传。
- Runtime 适配层只做协议翻译。
- 所有状态变化都通过统一命令和事件流完成。

### 6.4 让 Runtime 能力更标准化

**为什么重要**

不同 Runtime 能力不同。如果没有标准能力模型，产品会很难解释“为什么这个 Runtime 可以做，那个 Runtime 不可以做”。

**提升方向**

- 明确每个 Runtime 支持哪些能力。
- 统一错误类型和失败提示。
- 支持 Runtime 不可用时的降级方案。
- 让用户能理解不同 Runtime 的差异。

### 6.5 提升远程连接可靠性

**为什么重要**

KiKi 的关键体验依赖云端和本地机器连接。如果连接不稳定，用户会感知为“任务卡住”或“Agent 没反应”。

**提升方向**

- 更清楚地展示本地机器在线状态。
- 支持断线后的任务恢复。
- 对任务下发、执行中、结果回传建立确认机制。
- 明确区分网络问题、本地 Runtime 问题和模型问题。

### 6.6 提升执行过程可解释性

**为什么重要**

Agent 执行时间可能较长，用户最怕“不知道它在干什么”。

**提升方向**

- 展示当前处于哪个阶段：排队、运行、调用工具、生成文件、等待用户确认。
- 对长时间无输出给出原因解释。
- 把底层错误翻译成用户能理解的语言。
- 让用户能回看一次任务完整执行过程。

### 6.7 提升 Artifact 体验

**为什么重要**

KiKi 的最终价值不只是回答，而是交付成果。

**提升方向**

- 更准确识别哪些文件是最终产物。
- 支持更多文件类型预览。
- 标注产物来源、生成时间和对应任务。
- 对敏感文件和大文件做更严格治理。
- 支持用户继续基于产物发起下一步任务。

### 6.8 强化安全和隐私

**为什么重要**

KiKi 会接触本地代码、文件、命令和个人上下文，安全边界必须清晰。

**提升方向**

- 明确哪些数据只留本地，哪些可以上云。
- 对 shell、文件写入、网络访问建立审计记录。
- 在上传 Artifact 或写入记忆前做敏感信息过滤。
- 对高风险操作给用户更明确的提示。

---

## 7. 产品演进优先级

### P0：先保证系统可信和稳定

- 云端状态事实源稳定。
- 任务编排状态机清晰。
- 记忆不污染、不泄露、不乱写。
- 多 Runtime session 不串号。
- 本地连接和任务恢复可靠。
- Runtime 错误能明确反馈给用户。
- Artifact 不误传敏感数据。

### P1：提升用户可理解性

- 让用户知道 Agent 正在做什么。
- 让错误和卡住原因更清楚。
- 让任务、目标、收件箱之间的关系更清楚。
- 让产物更容易预览、下载和继续使用。
- 让收件箱成为待决策事项中心。

### P2：形成长期护城河

- 强化长期目标编排。
- 建立可治理的长期记忆。
- 接入更多 Runtime。
- 支持更复杂的跨任务成果聚合。
- 形成个人 Agent OS 的产品心智。

---

## 8. 最终结论

KiKi 的架构核心是：

> 产品体验层承接用户操作，目标与任务编排层负责推进长期目标，上下文与记忆层负责长期理解，云端控制层负责状态和调度，本地执行层负责真实工作，Runtime 适配层负责屏蔽不同 CLI 差异，事件、权限、Artifact 和观测能力贯穿全链路。

相比 OpenClaw，KiKi 的差异化不在于“能不能执行任务”，而在于：

- 是否围绕长期目标组织任务。
- 是否具备任务编排层。
- 是否具备可治理的记忆与上下文层。
- 是否具备云端控制面和长期状态。
- 是否能接入多个 Runtime。
- 是否能把本地产物回流成产品里的 Artifact。
- 是否能通过会话、目标、任务、收件箱和日程形成完整用户工作流。

后续最应该投入的是：任务编排、记忆治理、架构边界、连接可靠性、Runtime 标准化、执行可解释性、Artifact 治理和安全隐私。
