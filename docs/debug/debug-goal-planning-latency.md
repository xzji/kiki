[OPEN] goal-planning-latency

# 背景
- 现象：`/goal` 目标规划生成整体耗时偏长，用户希望拆解各阶段耗时并评估优化空间。
- 目标：获取真实阶段耗时分布，确认瓶颈位于 Claude 调用、串行编排、JSON 修复兜底，还是前后处理逻辑。

# 初始假设
1. 串行 Orchestrator 阶段过多，导致多次 Claude 往返时间线性叠加。
2. 某一阶段 Prompt 过长或上下文过大，导致单次 Claude 响应显著偏慢。
3. JSON 容错修复链路频繁触发，额外引入平衡截取、正则修复、Claude 兜底修复的二次耗时。
4. 子目标生成或任务生成按子目标逐个串行执行，缺少并行，放大总耗时。
5. 非模型耗时也不可忽略，例如 CLI 启动、环境准备、结果解析、Review 汇总存在额外开销。

# 计划
1. 梳理 `/goal` 规划实现链路与阶段边界。
2. 为关键阶段添加最小化耗时埋点，不改业务逻辑。
3. 复现一次规划生成，提取阶段耗时证据。
4. 基于证据给出优化优先级与改造建议。

# 当前证据
- 已在 `src/lib/server/goalPlanning.ts` 增加纯埋点日志：
  - Claude 调用开始/结束：记录 `promptChars`、`outputChars`、`elapsedMs`
  - JSON 解析命中路径：记录 `primary / balanced / common_repair / claude_repair`
  - 规划阶段耗时：整理背景信息、拆解子目标、每个子目标、最终汇总、总耗时
- 复现请求：`goal-plan-debug-latency`
- 复现结果：在 `Review 子目标 6/6` 之前被客户端超时中断，但已采集到绝大多数关键阶段耗时

# 首轮耗时结论
- 总时长（至中断前）：`1,199,455ms`，约 `19.99` 分钟
- Claude 主调用累计时长：`973,499ms`，约 `16.22` 分钟
- 非主调用耗时：`225,956ms`，约 `3.77` 分钟
- 已确认最重瓶颈：
  1. 多个子目标的 `生成任务 + review` 严格串行，耗时随子目标数线性增长
  2. `Review 子目标 1/6` 触发了 Claude JSON 修复，单次额外增加约 `193s`
  3. `拆解子目标` 单次耗时约 `110s`，属于较大的固定成本

# 阶段样本
- 整理背景信息：`66.8s`
- 拆解子目标：`109.7s`
- 子目标 1/6：`322.5s`，其中 review JSON 修复占主要额外开销
- 子目标 2/6：`117.1s`
- 子目标 3/6：`120.7s`
- 子目标 4/6：`231.8s`，其中 review 本身就较慢
- 子目标 5/6：`122.7s`

# 假设验证状态
1. 串行 Orchestrator 阶段过多：已确认
2. 某阶段 Prompt 过长导致整体变慢：部分成立，review Prompt 比任务生成更长，但不是唯一主因
3. JSON 容错修复链路频繁触发：至少本次发生 1 次，且代价极高
4. 子目标生成/Review 缺少并行：已确认
5. 非模型耗时占比显著：存在，但次于多轮 Claude 往返与修复调用
